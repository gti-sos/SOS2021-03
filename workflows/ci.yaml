name : CI
on: 
  push:
    branches: [ main ]

jobs :
  integrationTest: 
    runs-on: ubuntu-lastest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm start &
      - run: npx newman run test\integration-air-pollution.json
